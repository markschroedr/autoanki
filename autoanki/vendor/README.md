# Vendored KaTeX

`katex.min.js` is the browser bundle from the `katex` npm package, version
0.18.0. AutoAnki runs it inside QuickJS so math validation stays portable and
does not depend on a system-wide Node.js or KaTeX installation.

KaTeX is distributed under the MIT license in `KATEX_LICENSE.txt`.
